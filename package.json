{
  "name": "@onenomad/synapse-mcp",
  "version": "0.1.0",
  "description": "Synapse — cross-client bridge MCP for Claude Desktop ↔ Claude Code messaging. Ephemeral peer IDs, SQLite-backed mailbox, broadcast + direct messaging, threaded replies.",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "synapse-mcp": "./dist/cli.js"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "rm -rf dist && tsc",
    "dev": "tsc --watch",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
